import { useNavigate } from "react-router-dom"


const SignIn = () => {
    const navigate = useNavigate()
    return (
        <div onClick={() => {
        }}>

        </div>
    )
}

export {SignIn}